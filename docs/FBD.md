## It should generate a picture of the FBD schematic
- The image should be displayed instead of the FBD plotter.
- If you click the image you're taken to a full screen FBD Plotter.
- Remove the html sidepanel with the nodes.
- implement the same node listing as comfyui.


## Make the UI look and act like ComfyUI
- we've started on this already but we can emulate it more.
- hotkeys and things should match.
- mouse functions should match.
- zooming in and out.
- the nodes can still be more similar, for example being able to edit numbers directly on the nodes with the parameters being directly visisble.